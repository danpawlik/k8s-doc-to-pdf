# Script for generating PDF file with Kubernetes.io doc seciton.

How to run:

pip install -r requirements.txt
python generate-pdfs.py

In the future:
- merge multiple files into one
- scrap doc sections
